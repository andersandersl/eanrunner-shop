[build]
builder = "NIXPACKS"
buildCommand = "npm --prefix api install && npm --prefix api run build"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
